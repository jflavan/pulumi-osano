{
  "name": "osano",
  "displayName": "Osano (Unofficial)",
  "description": "Unofficial Pulumi provider for managing Osano Cookie Consent and Unified Consent resources. Not affiliated with Pulumi Corporation or Osano, Inc.",
  "keywords": [
    "pulumi",
    "osano",
    "consent",
    "cookie-consent",
    "privacy",
    "cmp",
    "category/infrastructure",
    "kind/native"
  ],
  "homepage": "https://github.com/jflavan/pulumi-osano",
  "license": "MIT",
  "repository": "https://github.com/jflavan/pulumi-osano",
  "logoUrl": "https://raw.githubusercontent.com/jflavan/pulumi-osano/main/assets/logo.png",
  "pluginDownloadURL": "github://api.github.com/jflavan/pulumi-osano",
  "publisher": "John Flavan",
  "namespace": "osano",
  "language": {
    "csharp": {
      "respectSchemaVersion": true,
      "rootNamespace": "Community.Pulumi"
    },
    "go": {
      "generateResourceContainerTypes": true,
      "importBasePath": "github.com/jflavan/pulumi-osano/sdk/go/osano",
      "respectSchemaVersion": true
    },
    "java": {
      "basePackage": "io.github.jflavan.pulumi",
      "buildFiles": "gradle"
    },
    "nodejs": {
      "packageDescription": "Pulumi provider for Osano Cookie Consent and Unified Consent APIs.",
      "packageName": "@jflavan/pulumi-osano",
      "respectSchemaVersion": true
    },
    "python": {
      "packageDescription": "Pulumi provider for Osano Cookie Consent and Unified Consent APIs.",
      "packageName": "pulumi_osano",
      "pyproject": {
        "enabled": true
      },
      "pythonRequires": ">=3.10",
      "respectSchemaVersion": true
    }
  },
  "config": {
    "variables": {
      "apiBaseUrl": {
        "type": "string",
        "description": "Base URL for the Osano Unified Consent API. Override only when targeting a custom domain (default https://uc.api.osano.com). OSANO_API_BASE_URL takes precedence when set."
      },
      "customerBaseUrl": {
        "type": "string",
        "description": "Override base URL for the Customer REST API (default: https://api.osano.com)."
      },
      "osanoApiKey": {
        "type": "string",
        "description": "Osano API key used for subject send-code/verify routes and Customer REST API/CMP operations (set via pulumi config set osano:osanoApiKey --secret, or OSANO_API_KEY, which takes precedence).",
        "secret": true
      },
      "requestTimeoutSeconds": {
        "type": "integer",
        "description": "HTTP request timeout in seconds for Customer REST API and Unified Consent calls (default 60). OSANO_API_TIMEOUT_SECONDS takes precedence when set to a positive integer."
      },
      "ucApiKey": {
        "type": "string",
        "description": "Unified Consent API key for the Unified Consent Core API (x-uc-api-key).",
        "deprecationMessage": "use unifiedConsentApiKey instead",
        "secret": true
      },
      "ucBaseUrl": {
        "type": "string",
        "description": "Override base URL for the Unified Consent Core API (default: https://uc.api.osano.com).",
        "deprecationMessage": "use apiBaseUrl instead"
      },
      "unifiedConsentApiKey": {
        "type": "string",
        "description": "Unified Consent API key used for consent collection routes (set via pulumi config set osano:unifiedConsentApiKey --secret, or OSANO_UC_API_KEY, which takes precedence).",
        "secret": true
      }
    }
  },
  "types": {
    "osano:index:ConsentAction": {
      "properties": {
        "action": {
          "type": "string",
          "description": "The subject's choice: ACCEPT, REJECT, or UNSELECTED."
        },
        "jurisdiction": {
          "type": "string",
          "description": "Optional jurisdiction for this action; overrides the top-level jurisdiction."
        },
        "target": {
          "type": "string",
          "description": "The privacy protocol ID (the Target ID on the privacy protocol's edit page)."
        },
        "vendor": {
          "type": "string",
          "description": "The Unified Consent configuration ID the consent is recorded for."
        }
      },
      "type": "object",
      "required": [
        "target",
        "vendor",
        "action"
      ]
    },
    "osano:index:ConsentCompliance": {
      "properties": {
        "gpc": {
          "type": "integer",
          "description": "1 if the Global Privacy Control signal is enabled, 0 otherwise."
        },
        "privacyPolicy": {
          "$ref": "#/types/osano:index:ConsentPrivacyPolicy",
          "description": "The privacy policy in effect when the consent was given."
        }
      },
      "type": "object"
    },
    "osano:index:ConsentPrivacyPolicy": {
      "properties": {
        "url": {
          "type": "string",
          "description": "The privacy policy URL."
        },
        "version": {
          "type": "string",
          "description": "The privacy policy version active when the consent was submitted."
        }
      },
      "type": "object",
      "required": [
        "url"
      ]
    },
    "osano:index:ConsentSubject": {
      "properties": {
        "anonymousId": {
          "type": "string",
          "description": "The subject's anonymous ID. Must not contain #, %, or spaces."
        },
        "verifiedId": {
          "type": "string",
          "description": "The subject's verified ID. Must not contain #, %, or spaces."
        }
      },
      "type": "object"
    },
    "osano:index:CookieConsentAuditEvent": {
      "properties": {
        "actor": {
          "type": "string",
          "description": "The email of the user who performed the action, when known."
        },
        "eventType": {
          "type": "string",
          "description": "The machine-readable event type, such as cmp.configPublished."
        },
        "id": {
          "type": "string",
          "description": "The audit event ID."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "Event-specific details, such as changed fields and before/after values."
        },
        "module": {
          "type": "string",
          "description": "The Osano module, currently always CMP."
        },
        "resources": {
          "type": "array",
          "items": {
            "$ref": "#/types/osano:index:CookieConsentAuditResource"
          },
          "description": "The resources the event acted on."
        },
        "timestamp": {
          "type": "string",
          "description": "When the event occurred (UTC ISO 8601)."
        }
      },
      "type": "object",
      "required": [
        "id",
        "module",
        "eventType",
        "timestamp",
        "resources"
      ]
    },
    "osano:index:CookieConsentAuditResource": {
      "properties": {
        "isPrimary": {
          "type": "boolean",
          "description": "Whether this is the event's primary resource."
        },
        "resourceId": {
          "type": "string",
          "description": "The resource ID; for CMP events, the config ID."
        },
        "resourceName": {
          "type": "string",
          "description": "The resource name, when known."
        },
        "resourceType": {
          "type": "string",
          "description": "The resource type, such as CMP."
        }
      },
      "type": "object",
      "required": [
        "resourceId",
        "resourceType",
        "isPrimary"
      ]
    },
    "osano:index:CookieConsentConfigDetails": {
      "properties": {
        "configId": {
          "type": "string",
          "description": "The Osano config ID (UUID)."
        },
        "configuration": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "The CMP configuration object as Osano reports it, including server defaults."
        },
        "created": {
          "type": "integer",
          "description": "Unix timestamp when Osano created the configuration."
        },
        "customerId": {
          "type": "string",
          "description": "The Osano customer ID that owns the configuration."
        },
        "domains": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Domains permitted to host the configuration."
        },
        "lastPublished": {
          "type": "integer",
          "description": "Unix timestamp when Osano last published the configuration (0 if never)."
        },
        "mode": {
          "type": "string",
          "description": "Compliance mode: debug, permissive, or production."
        },
        "name": {
          "type": "string",
          "description": "The configuration name."
        },
        "orgIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Organization IDs associated with the configuration."
        },
        "publishStatus": {
          "type": "string",
          "description": "Osano publication status: unpublished, in-progress, published, outdated (changed since the last publish), or error."
        },
        "publishedRevision": {
          "type": "integer",
          "description": "Revision number most recently published by Osano."
        },
        "scriptSrc": {
          "type": "string",
          "description": "The public hosted CMP JavaScript URL, https://cmp.osano.com/{customerId}/{configId}/osano.js. It serves the most recently published revision."
        },
        "scriptTag": {
          "type": "string",
          "description": "The complete public CMP script tag to place first in the site head, without async or defer attributes."
        },
        "tattleRecordStopped": {
          "type": "boolean",
          "description": "Whether Osano stopped recording discoveries (tattles) for the configuration."
        },
        "updated": {
          "type": "integer",
          "description": "Unix timestamp when Osano last updated the configuration."
        }
      },
      "type": "object",
      "required": [
        "configId",
        "name",
        "domains",
        "mode",
        "orgIds",
        "configuration",
        "customerId",
        "created",
        "updated",
        "publishStatus",
        "lastPublished",
        "publishedRevision",
        "tattleRecordStopped",
        "scriptSrc",
        "scriptTag"
      ]
    },
    "osano:index:CookieConsentDiscovery": {
      "properties": {
        "confidence": {
          "type": "string",
          "description": "Osano's AI classification confidence (Unknown, Low, Medium, or High). Only reported for cookies."
        },
        "created": {
          "type": "string",
          "description": "When the discovery was first seen (ISO 8601)."
        },
        "firstPageSeen": {
          "type": "string",
          "description": "The page URL where the item was first seen."
        },
        "scanOrigin": {
          "type": "string",
          "description": "\"URL Scan\" or \"osano.js\"; unset when Osano does not know the origin."
        },
        "storeKey": {
          "type": "string",
          "description": "The discovered cookie name, script or iframe URL, or localStorage key."
        },
        "storeType": {
          "type": "string",
          "description": "The discovery's storage type as Osano reports it."
        },
        "updated": {
          "type": "string",
          "description": "When the discovery was last updated (ISO 8601)."
        }
      },
      "type": "object",
      "required": [
        "storeKey",
        "storeType",
        "created",
        "updated",
        "firstPageSeen"
      ]
    },
    "osano:index:CookieConsentRuleDetails": {
      "properties": {
        "classification": {
          "type": "string",
          "description": "The rule classification."
        },
        "configId": {
          "type": "string",
          "description": "The configuration the rule belongs to."
        },
        "created": {
          "type": "string",
          "description": "When the rule was created (ISO 8601)."
        },
        "description": {
          "type": "string",
          "description": "The cookie description, if set (cookies only)."
        },
        "disclosure": {
          "type": "boolean",
          "description": "Whether the rule is disclosed."
        },
        "expiry": {
          "type": "string",
          "description": "The cookie expiry description, if set (cookies only)."
        },
        "rule": {
          "type": "string",
          "description": "The rule pattern."
        },
        "ruleId": {
          "type": "integer",
          "description": "The server-assigned integer rule ID. Import a rule with <configId>/<ruleId>."
        },
        "ruleType": {
          "type": "string",
          "description": "The matching mode, if set."
        },
        "storeType": {
          "type": "string",
          "description": "The storage type: cookies, scripts, iframes, or localStorage. Osano's raw type is passed through when it is not one of these."
        },
        "title": {
          "type": "string",
          "description": "The disclosure title, if set."
        },
        "updated": {
          "type": "string",
          "description": "When the rule was last updated (ISO 8601)."
        },
        "vendorId": {
          "type": "string",
          "description": "The Osano vendor ID, if set."
        },
        "vendorName": {
          "type": "string",
          "description": "The vendor name, if set."
        }
      },
      "type": "object",
      "required": [
        "ruleId",
        "configId",
        "storeType",
        "classification",
        "rule",
        "disclosure",
        "created",
        "updated"
      ]
    }
  },
  "provider": {
    "properties": {
      "apiBaseUrl": {
        "type": "string",
        "description": "Base URL for the Osano Unified Consent API. Override only when targeting a custom domain (default https://uc.api.osano.com). OSANO_API_BASE_URL takes precedence when set."
      },
      "customerBaseUrl": {
        "type": "string",
        "description": "Override base URL for the Customer REST API (default: https://api.osano.com)."
      },
      "osanoApiKey": {
        "type": "string",
        "description": "Osano API key used for subject send-code/verify routes and Customer REST API/CMP operations (set via pulumi config set osano:osanoApiKey --secret, or OSANO_API_KEY, which takes precedence).",
        "secret": true
      },
      "requestTimeoutSeconds": {
        "type": "integer",
        "description": "HTTP request timeout in seconds for Customer REST API and Unified Consent calls (default 60). OSANO_API_TIMEOUT_SECONDS takes precedence when set to a positive integer."
      },
      "ucApiKey": {
        "type": "string",
        "description": "Unified Consent API key for the Unified Consent Core API (x-uc-api-key).",
        "deprecationMessage": "use unifiedConsentApiKey instead",
        "secret": true
      },
      "ucBaseUrl": {
        "type": "string",
        "description": "Override base URL for the Unified Consent Core API (default: https://uc.api.osano.com).",
        "deprecationMessage": "use apiBaseUrl instead"
      },
      "unifiedConsentApiKey": {
        "type": "string",
        "description": "Unified Consent API key used for consent collection routes (set via pulumi config set osano:unifiedConsentApiKey --secret, or OSANO_UC_API_KEY, which takes precedence).",
        "secret": true
      }
    },
    "inputProperties": {
      "apiBaseUrl": {
        "type": "string",
        "description": "Base URL for the Osano Unified Consent API. Override only when targeting a custom domain (default https://uc.api.osano.com). OSANO_API_BASE_URL takes precedence when set."
      },
      "customerBaseUrl": {
        "type": "string",
        "description": "Override base URL for the Customer REST API (default: https://api.osano.com)."
      },
      "osanoApiKey": {
        "type": "string",
        "description": "Osano API key used for subject send-code/verify routes and Customer REST API/CMP operations (set via pulumi config set osano:osanoApiKey --secret, or OSANO_API_KEY, which takes precedence).",
        "secret": true
      },
      "requestTimeoutSeconds": {
        "type": "integer",
        "description": "HTTP request timeout in seconds for Customer REST API and Unified Consent calls (default 60). OSANO_API_TIMEOUT_SECONDS takes precedence when set to a positive integer."
      },
      "ucApiKey": {
        "type": "string",
        "description": "Unified Consent API key for the Unified Consent Core API (x-uc-api-key).",
        "deprecationMessage": "use unifiedConsentApiKey instead",
        "secret": true
      },
      "ucBaseUrl": {
        "type": "string",
        "description": "Override base URL for the Unified Consent Core API (default: https://uc.api.osano.com).",
        "deprecationMessage": "use apiBaseUrl instead"
      },
      "unifiedConsentApiKey": {
        "type": "string",
        "description": "Unified Consent API key used for consent collection routes (set via pulumi config set osano:unifiedConsentApiKey --secret, or OSANO_UC_API_KEY, which takes precedence).",
        "secret": true
      }
    }
  },
  "resources": {
    "osano:index:Consent": {
      "description": "Submits a Unified Consent decision for a subject. Consents are immutable in Osano: changing any input submits a new consent (replacement), and destroying the resource only removes it from Pulumi state. Set origin to gpc and omit actions to submit a Global Privacy Control consent, whose actions Osano derives and returns in gpcActions.",
      "properties": {
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/types/osano:index:ConsentAction"
          },
          "description": "Consent actions referencing privacy protocols (target) within a configuration (vendor). Required unless origin is gpc.",
          "replaceOnChanges": true
        },
        "attributes": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Optional key/value attributes stored with the consent record. Osano fills ipAddress and userAgent itself and overwrites values sent for those keys.",
          "replaceOnChanges": true
        },
        "compliance": {
          "$ref": "#/types/osano:index:ConsentCompliance",
          "description": "Optional compliance metadata such as the privacy policy version and GPC signal.",
          "replaceOnChanges": true
        },
        "consentId": {
          "type": "string",
          "description": "Synthetic identifier used by Pulumi to track consent submissions."
        },
        "countryCodeOverride": {
          "type": "string",
          "description": "Optional ISO 3166-1 country code Osano uses instead of resolving the caller's IP address. Set it when submitting from a pipeline, whose IP address says nothing about the subject.",
          "replaceOnChanges": true
        },
        "gpcActions": {
          "type": "array",
          "items": {
            "$ref": "#/types/osano:index:ConsentAction"
          },
          "description": "The actions Osano derived for a GPC consent submitted without actions."
        },
        "jurisdiction": {
          "type": "string",
          "description": "Optional jurisdiction, which must be one of the configuration's jurisdictions (see getCollections).",
          "replaceOnChanges": true
        },
        "lastSynced": {
          "type": "string",
          "description": "Timestamp of the last refresh from the Osano API (RFC3339)."
        },
        "origin": {
          "type": "string",
          "description": "Origin of the consent: api (default) or gpc. With gpc and no actions, the consent is submitted to Osano's GPC endpoint, which derives the actions.",
          "replaceOnChanges": true
        },
        "regionCodeOverride": {
          "type": "string",
          "description": "Optional ISO 3166-2 region code Osano uses instead of resolving the caller's IP address.",
          "replaceOnChanges": true
        },
        "sessionToken": {
          "type": "string",
          "description": "Optional session token returned when the subject's profile was created.",
          "secret": true,
          "replaceOnChanges": true
        },
        "subject": {
          "$ref": "#/types/osano:index:ConsentSubject",
          "description": "Subject identifiers used for the consent (verifiedId or anonymousId).",
          "replaceOnChanges": true
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Custom tags that Osano associates with the consent record.",
          "replaceOnChanges": true
        }
      },
      "required": [
        "subject",
        "consentId",
        "lastSynced"
      ],
      "inputProperties": {
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/types/osano:index:ConsentAction"
          },
          "description": "Consent actions referencing privacy protocols (target) within a configuration (vendor). Required unless origin is gpc.",
          "replaceOnChanges": true
        },
        "attributes": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Optional key/value attributes stored with the consent record. Osano fills ipAddress and userAgent itself and overwrites values sent for those keys.",
          "replaceOnChanges": true
        },
        "compliance": {
          "$ref": "#/types/osano:index:ConsentCompliance",
          "description": "Optional compliance metadata such as the privacy policy version and GPC signal.",
          "replaceOnChanges": true
        },
        "countryCodeOverride": {
          "type": "string",
          "description": "Optional ISO 3166-1 country code Osano uses instead of resolving the caller's IP address. Set it when submitting from a pipeline, whose IP address says nothing about the subject.",
          "replaceOnChanges": true
        },
        "jurisdiction": {
          "type": "string",
          "description": "Optional jurisdiction, which must be one of the configuration's jurisdictions (see getCollections).",
          "replaceOnChanges": true
        },
        "origin": {
          "type": "string",
          "description": "Origin of the consent: api (default) or gpc. With gpc and no actions, the consent is submitted to Osano's GPC endpoint, which derives the actions.",
          "replaceOnChanges": true
        },
        "regionCodeOverride": {
          "type": "string",
          "description": "Optional ISO 3166-2 region code Osano uses instead of resolving the caller's IP address.",
          "replaceOnChanges": true
        },
        "sessionToken": {
          "type": "string",
          "description": "Optional session token returned when the subject's profile was created.",
          "secret": true,
          "replaceOnChanges": true
        },
        "subject": {
          "$ref": "#/types/osano:index:ConsentSubject",
          "description": "Subject identifiers used for the consent (verifiedId or anonymousId).",
          "replaceOnChanges": true
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Custom tags that Osano associates with the consent record.",
          "replaceOnChanges": true
        }
      },
      "requiredInputs": [
        "subject"
      ]
    },
    "osano:index:CookieConsentConfig": {
      "description": "Manages an Osano Cookie Consent (CMP) configuration. Import with the Osano config ID. Osano has no delete endpoint for configs, so deleting this resource only removes it from Pulumi state and retains the upstream configuration.",
      "properties": {
        "configId": {
          "type": "string",
          "description": "The Osano configId (UUID)."
        },
        "configuration": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "CMP configuration object. At minimum, must include storagePolicyHref."
        },
        "created": {
          "type": "integer",
          "description": "Unix timestamp when Osano created the configuration."
        },
        "customerId": {
          "type": "string",
          "description": "The Osano customer ID that owns the configuration."
        },
        "domains": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Domains permitted to host the configuration."
        },
        "lastPublished": {
          "type": "integer",
          "description": "Unix timestamp when Osano last published the configuration."
        },
        "mode": {
          "type": "string",
          "description": "Compliance mode: debug, permissive, or production."
        },
        "name": {
          "type": "string",
          "description": "The name of the configuration."
        },
        "orgIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional organization IDs associated with the config."
        },
        "publishStatus": {
          "type": "string",
          "description": "Current Osano publication status for the configuration."
        },
        "publishedRevision": {
          "type": "integer",
          "description": "Revision number most recently published by Osano."
        },
        "tattleRecordStopped": {
          "type": "boolean",
          "description": "Whether Osano stopped recording discoveries (tattles) for the configuration."
        },
        "updated": {
          "type": "integer",
          "description": "Unix timestamp when Osano last updated the configuration."
        }
      },
      "required": [
        "name",
        "domains",
        "mode",
        "configuration",
        "configId"
      ],
      "inputProperties": {
        "configuration": {
          "type": "object",
          "additionalProperties": {
            "$ref": "pulumi.json#/Any"
          },
          "description": "CMP configuration object. At minimum, must include storagePolicyHref."
        },
        "domains": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Domains permitted to host the configuration."
        },
        "mode": {
          "type": "string",
          "description": "Compliance mode: debug, permissive, or production."
        },
        "name": {
          "type": "string",
          "description": "The name of the configuration."
        },
        "orgIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional organization IDs associated with the config."
        }
      },
      "requiredInputs": [
        "name",
        "domains",
        "mode",
        "configuration"
      ]
    },
    "osano:index:CookieConsentPublication": {
      "description": "Publishes an Osano Cookie Consent configuration, waits for completion, and returns its hosted CMP script. Import with the Osano config ID; deleting this resource only removes Pulumi state and does not unpublish the config.",
      "properties": {
        "changeToken": {
          "type": "string",
          "description": "A caller-managed desired-state token. Changing it queues a new publication. Changing keepUnclassifiedTattles, description, or webhookUrl also republishes; when no input changes, nothing is published."
        },
        "configId": {
          "type": "string",
          "description": "The Osano Cookie Consent config ID to publish. This is also the import ID."
        },
        "customerId": {
          "type": "string",
          "description": "The Osano customer ID that owns the published configuration."
        },
        "description": {
          "type": "string",
          "description": "Optional description sent with the publication request."
        },
        "keepUnclassifiedTattles": {
          "type": "boolean",
          "description": "Whether publication preserves unclassified discoveries. Defaults to true to avoid unexpected deletion.",
          "default": true
        },
        "lastPublished": {
          "type": "integer",
          "description": "Unix timestamp of the completed Osano publication."
        },
        "publishStatus": {
          "type": "string",
          "description": "The Osano publication status: published after a completed publish, and possibly outdated after a refresh when the config changed outside a publish."
        },
        "publishedRevision": {
          "type": "integer",
          "description": "The configuration revision most recently published by Osano."
        },
        "scriptSrc": {
          "type": "string",
          "description": "The public hosted Osano CMP JavaScript URL for this customer and config."
        },
        "scriptTag": {
          "type": "string",
          "description": "The complete public CMP script tag to place first in the site head, without async or defer attributes."
        },
        "webhookUrl": {
          "type": "string",
          "description": "Optional absolute HTTP or HTTPS URL Osano calls when the publication completes. Osano does not document the call's payload or sign it, so use an unguessable URL; the value is stored as a secret.",
          "secret": true
        }
      },
      "required": [
        "configId",
        "changeToken",
        "keepUnclassifiedTattles",
        "customerId",
        "publishStatus",
        "lastPublished",
        "publishedRevision",
        "scriptSrc",
        "scriptTag"
      ],
      "inputProperties": {
        "changeToken": {
          "type": "string",
          "description": "A caller-managed desired-state token. Changing it queues a new publication. Changing keepUnclassifiedTattles, description, or webhookUrl also republishes; when no input changes, nothing is published."
        },
        "configId": {
          "type": "string",
          "description": "The Osano Cookie Consent config ID to publish. This is also the import ID."
        },
        "description": {
          "type": "string",
          "description": "Optional description sent with the publication request."
        },
        "keepUnclassifiedTattles": {
          "type": "boolean",
          "description": "Whether publication preserves unclassified discoveries. Defaults to true to avoid unexpected deletion.",
          "default": true
        },
        "webhookUrl": {
          "type": "string",
          "description": "Optional absolute HTTP or HTTPS URL Osano calls when the publication completes. Osano does not document the call's payload or sign it, so use an unguessable URL; the value is stored as a secret.",
          "secret": true
        }
      },
      "requiredInputs": [
        "configId",
        "changeToken"
      ]
    },
    "osano:index:CookieConsentRule": {
      "description": "Manages an Osano Cookie Consent (CMP) rule within a configuration. Import with `<configId>/<ruleId>`. Changing configId or storeType replaces the rule, and deleting this resource deletes the rule in Osano.",
      "properties": {
        "classification": {
          "type": "string",
          "description": "Classification: ANALYTICS, BLACKLISTED, ESSENTIAL, HIDDEN, MARKETING, or PERSONALIZATION."
        },
        "configId": {
          "type": "string",
          "description": "The configId of the Cookie Consent Configuration this rule belongs to."
        },
        "created": {
          "type": "string",
          "description": "Timestamp when the rule was created."
        },
        "description": {
          "type": "string",
          "description": "Optional cookie description. Only supported for cookies; max 1000 characters."
        },
        "disclosure": {
          "type": "boolean",
          "description": "Whether the rule should be disclosed. Defaults to false."
        },
        "expiry": {
          "type": "string",
          "description": "Optional cookie expiry description. Only supported for cookies; max 50 characters."
        },
        "rule": {
          "type": "string",
          "description": "The rule pattern (e.g. a cookie name pattern). Min 3, max 1000 characters."
        },
        "ruleId": {
          "type": "integer",
          "description": "The server-assigned integer rule ID."
        },
        "ruleType": {
          "type": "string",
          "description": "Optional matching mode: FILENAME, DOMAIN, PATH, REGEXP, STARTS_WITH, ENDS_WITH, CONTAINS, or EXACT_MATCH."
        },
        "storeType": {
          "type": "string",
          "description": "The storage type category: cookies, scripts, iframes, or localStorage."
        },
        "title": {
          "type": "string",
          "description": "Optional title for the rule, used in consent disclosure. Max 64 characters."
        },
        "updated": {
          "type": "string",
          "description": "Timestamp when the rule was last updated."
        },
        "vendorName": {
          "type": "string",
          "description": "Optional vendor name for the rule. Max 100 characters."
        }
      },
      "required": [
        "configId",
        "storeType",
        "classification",
        "rule",
        "ruleId"
      ],
      "inputProperties": {
        "classification": {
          "type": "string",
          "description": "Classification: ANALYTICS, BLACKLISTED, ESSENTIAL, HIDDEN, MARKETING, or PERSONALIZATION."
        },
        "configId": {
          "type": "string",
          "description": "The configId of the Cookie Consent Configuration this rule belongs to."
        },
        "description": {
          "type": "string",
          "description": "Optional cookie description. Only supported for cookies; max 1000 characters."
        },
        "disclosure": {
          "type": "boolean",
          "description": "Whether the rule should be disclosed. Defaults to false."
        },
        "expiry": {
          "type": "string",
          "description": "Optional cookie expiry description. Only supported for cookies; max 50 characters."
        },
        "rule": {
          "type": "string",
          "description": "The rule pattern (e.g. a cookie name pattern). Min 3, max 1000 characters."
        },
        "ruleType": {
          "type": "string",
          "description": "Optional matching mode: FILENAME, DOMAIN, PATH, REGEXP, STARTS_WITH, ENDS_WITH, CONTAINS, or EXACT_MATCH."
        },
        "storeType": {
          "type": "string",
          "description": "The storage type category: cookies, scripts, iframes, or localStorage."
        },
        "title": {
          "type": "string",
          "description": "Optional title for the rule, used in consent disclosure. Max 64 characters."
        },
        "vendorName": {
          "type": "string",
          "description": "Optional vendor name for the rule. Max 100 characters."
        }
      },
      "requiredInputs": [
        "configId",
        "storeType",
        "classification",
        "rule"
      ]
    }
  },
  "functions": {
    "osano:index:checkConsent": {
      "description": "Checks whether a unified consent record exists for a given subject ID.",
      "inputs": {
        "properties": {
          "countryCodeOverride": {
            "type": "string",
            "description": "Optional ISO 3166-1 country code Osano uses instead of resolving the caller's IP address, which in a pipeline is the CI runner's."
          },
          "regionCodeOverride": {
            "type": "string",
            "description": "Optional ISO 3166-2 region code Osano uses instead of resolving the caller's IP address."
          },
          "subjectId": {
            "type": "string",
            "description": "The subject ID to check."
          }
        },
        "type": "object",
        "required": [
          "subjectId"
        ]
      },
      "outputs": {
        "properties": {
          "exists": {
            "description": "Whether the subject has given consent in the configuration.",
            "type": "boolean"
          },
          "subjectId": {
            "description": "The subject ID that was checked.",
            "type": "string"
          }
        },
        "required": [
          "subjectId",
          "exists"
        ],
        "type": "object"
      }
    },
    "osano:index:getCollection": {
      "description": "Retrieves a specific privacy protocol collection by ID.",
      "inputs": {
        "properties": {
          "collectionId": {
            "type": "string",
            "description": "The privacy protocol collection ID to fetch."
          }
        },
        "type": "object",
        "required": [
          "collectionId"
        ]
      },
      "outputs": {
        "properties": {
          "collection": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The collection: collectionId, name, frameworks, configIds, jurisdiction, type, consents, and preferences.",
            "type": "object"
          },
          "collectionId": {
            "description": "The collection ID that was looked up.",
            "type": "string"
          },
          "exists": {
            "description": "Whether Osano returned the collection.",
            "type": "boolean"
          }
        },
        "required": [
          "collectionId",
          "collection",
          "exists"
        ],
        "type": "object"
      }
    },
    "osano:index:getCollections": {
      "description": "Retrieves the aggregated privacy protocol collections, optionally filtered by jurisdiction and type.",
      "inputs": {
        "properties": {
          "jurisdiction": {
            "type": "string",
            "description": "Optional jurisdiction filter. When unset, Osano resolves the jurisdiction from the caller's IP address."
          },
          "type": {
            "type": "string",
            "description": "Optional collection type: published (default) or draft."
          }
        },
        "type": "object"
      },
      "outputs": {
        "properties": {
          "collection": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The collection of privacy protocols that applies to the jurisdiction.",
            "type": "object"
          },
          "jurisdictions": {
            "description": "Every jurisdiction the configuration defines.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "jurisdictions",
          "collection"
        ],
        "type": "object"
      }
    },
    "osano:index:getConfig": {
      "description": "Retrieves the Unified Consent configuration referenced by the API key.",
      "inputs": {
        "type": "object"
      },
      "outputs": {
        "properties": {
          "config": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The Unified Consent configuration: configId, customerId, name, domains, privacy policy, privacyProtocols, frameworks, styling, publication state, and text customizations.",
            "type": "object"
          }
        },
        "required": [
          "config"
        ],
        "type": "object"
      }
    },
    "osano:index:getConsentProfile": {
      "description": "Retrieves a consent profile for a hashed subject identifier and config ID.",
      "inputs": {
        "properties": {
          "configId": {
            "type": "string",
            "description": "The consent configuration ID the profile belongs to."
          },
          "countryCodeOverride": {
            "type": "string",
            "description": "Optional ISO 3166-1 country code Osano uses instead of resolving the caller's IP address, which in a pipeline is the CI runner's."
          },
          "hashedSubjectId": {
            "type": "string",
            "description": "The hashed subject identifier whose consent profile is returned."
          },
          "regionCodeOverride": {
            "type": "string",
            "description": "Optional ISO 3166-2 region code Osano uses instead of resolving the caller's IP address."
          }
        },
        "type": "object",
        "required": [
          "hashedSubjectId",
          "configId"
        ]
      },
      "outputs": {
        "properties": {
          "configId": {
            "description": "The configuration ID that was looked up.",
            "type": "string"
          },
          "exists": {
            "description": "Whether Osano returned a consent profile.",
            "type": "boolean"
          },
          "hashedSubjectId": {
            "description": "The hashed subject identifier that was looked up.",
            "type": "string"
          },
          "profile": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The consent profile Osano returned, with unifiedConsent and conflicts keys.",
            "type": "object"
          }
        },
        "required": [
          "hashedSubjectId",
          "configId",
          "profile",
          "exists"
        ],
        "type": "object"
      }
    },
    "osano:index:getCookieConsentAuditLog": {
      "description": "Queries the Cookie Consent audit log: configuration and rule changes and publications, with who made them and when. Use it to confirm a pipeline's publication or to detect dashboard edits.",
      "inputs": {
        "properties": {
          "actor": {
            "type": "string",
            "description": "Only return events performed by this user email (case insensitive)."
          },
          "changeType": {
            "type": "string",
            "description": "Only return change events of this type: text_customization, style, iab, setting, or rule."
          },
          "configIds": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Only return events for these configuration IDs."
          },
          "endDate": {
            "type": "string",
            "description": "Only return events strictly before this UTC ISO 8601 timestamp."
          },
          "eventIds": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Only return these audit event IDs, such as the changeIds of a cmp.configPublished event."
          },
          "eventTypes": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Only return these event types, for example cmp.configPublished, cmp.configUpdated, cmp.ruleCreated, or cmp.ruleUpdated."
          },
          "maxResults": {
            "type": "integer",
            "description": "Stop after this many events (most recent first). Defaults to 200; set 0 to return every matching event."
          },
          "startDate": {
            "type": "string",
            "description": "Only return events at or after this UTC ISO 8601 timestamp."
          }
        },
        "type": "object"
      },
      "outputs": {
        "properties": {
          "events": {
            "description": "The matching events, most recent first.",
            "items": {
              "$ref": "#/types/osano:index:CookieConsentAuditEvent"
            },
            "type": "array"
          }
        },
        "required": [
          "events"
        ],
        "type": "object"
      }
    },
    "osano:index:getCookieConsentConfig": {
      "description": "Reads a Cookie Consent configuration, including its publish status and the CMP script to install, without managing it. Use it to fetch the script for a configuration another stack or the Osano dashboard owns. The script serves the most recently published revision, so check publishStatus and lastPublished before relying on a new change.",
      "inputs": {
        "properties": {
          "configId": {
            "type": "string",
            "description": "The Osano Cookie Consent config ID (UUID) to read."
          }
        },
        "type": "object",
        "required": [
          "configId"
        ]
      },
      "outputs": {
        "properties": {
          "configId": {
            "description": "The config ID that was looked up.",
            "type": "string"
          },
          "configuration": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The CMP configuration object as Osano reports it, including server defaults.",
            "type": "object"
          },
          "created": {
            "description": "Unix timestamp when Osano created the configuration.",
            "type": "integer"
          },
          "customerId": {
            "description": "The Osano customer ID that owns the configuration.",
            "type": "string"
          },
          "domains": {
            "description": "Domains permitted to host the configuration.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "exists": {
            "description": "Whether Osano returned the configuration. The other outputs are empty when false.",
            "type": "boolean"
          },
          "lastPublished": {
            "description": "Unix timestamp when Osano last published the configuration (0 if never).",
            "type": "integer"
          },
          "mode": {
            "description": "Compliance mode: debug, permissive, or production.",
            "type": "string"
          },
          "name": {
            "description": "The configuration name.",
            "type": "string"
          },
          "orgIds": {
            "description": "Organization IDs associated with the configuration.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "publishStatus": {
            "description": "Osano publication status: unpublished, in-progress, published, outdated (changed since the last publish), or error.",
            "type": "string"
          },
          "publishedRevision": {
            "description": "Revision number most recently published by Osano.",
            "type": "integer"
          },
          "scriptSrc": {
            "description": "The public hosted CMP JavaScript URL, https://cmp.osano.com/{customerId}/{configId}/osano.js. It serves the most recently published revision.",
            "type": "string"
          },
          "scriptTag": {
            "description": "The complete public CMP script tag to place first in the site head, without async or defer attributes.",
            "type": "string"
          },
          "tattleRecordStopped": {
            "description": "Whether Osano stopped recording discoveries (tattles) for the configuration.",
            "type": "boolean"
          },
          "updated": {
            "description": "Unix timestamp when Osano last updated the configuration.",
            "type": "integer"
          }
        },
        "required": [
          "configId",
          "exists",
          "name",
          "domains",
          "mode",
          "orgIds",
          "configuration",
          "customerId",
          "created",
          "updated",
          "publishStatus",
          "lastPublished",
          "publishedRevision",
          "tattleRecordStopped",
          "scriptSrc",
          "scriptTag"
        ],
        "type": "object"
      }
    },
    "osano:index:getCookieConsentConfigs": {
      "description": "Lists Cookie Consent configurations, optionally filtered by name, domains, organization, mode, or publish status, and returns each one's install script. Follows pagination until maxResults or the last page.",
      "inputs": {
        "properties": {
          "domains": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Filter by domains. Use Punycode for non-ASCII domains."
          },
          "domainsMatch": {
            "type": "string",
            "description": "How domains filters: any (default) matches configs with any listed domain, all requires every listed domain, and not excludes configs with any listed domain."
          },
          "maxResults": {
            "type": "integer",
            "description": "Stop after this many configurations. Unset or 0 returns every matching configuration."
          },
          "mode": {
            "type": "string",
            "description": "Filter by compliance mode: debug, permissive, or production."
          },
          "name": {
            "type": "string",
            "description": "Filter by configuration name (case insensitive, partial matches allowed)."
          },
          "orgIds": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Filter by organization IDs (UUIDs)."
          },
          "orgIdsMatch": {
            "type": "string",
            "description": "How orgIds filters: any (default), all, or not, as for domainsMatch."
          },
          "publishStatus": {
            "type": "string",
            "description": "Filter by publish status: unpublished, in-progress, published, outdated, or error."
          },
          "sortBy": {
            "type": "string",
            "description": "Sort field: name, created (default), updated, or lastPublished. Results are in descending order."
          },
          "tattleRecordStopped": {
            "type": "boolean",
            "description": "Filter by whether discovery recording is stopped."
          }
        },
        "type": "object"
      },
      "outputs": {
        "properties": {
          "configs": {
            "description": "The matching configurations, each with its install script.",
            "items": {
              "$ref": "#/types/osano:index:CookieConsentConfigDetails"
            },
            "type": "array"
          }
        },
        "required": [
          "configs"
        ],
        "type": "object"
      }
    },
    "osano:index:getCookieConsentDiscoveries": {
      "description": "Lists the cookies, scripts, iframes, or localStorage keys that osano.js or URL scans discovered for a configuration. Use it to review what still needs a rule before switching a configuration to production mode, which blocks everything unclassified.",
      "inputs": {
        "properties": {
          "configId": {
            "type": "string",
            "description": "The Osano Cookie Consent config ID whose discoveries are listed."
          },
          "storeType": {
            "type": "string",
            "description": "The storage type to list: cookies (default, as in the Osano API), scripts, iframes, or localStorage."
          }
        },
        "type": "object",
        "required": [
          "configId"
        ]
      },
      "outputs": {
        "properties": {
          "configId": {
            "description": "The config ID that was queried.",
            "type": "string"
          },
          "discoveries": {
            "description": "The discoveries Osano reports for the configuration and storage type.",
            "items": {
              "$ref": "#/types/osano:index:CookieConsentDiscovery"
            },
            "type": "array"
          },
          "storeType": {
            "description": "The storage type that was listed.",
            "type": "string"
          }
        },
        "required": [
          "configId",
          "storeType",
          "discoveries"
        ],
        "type": "object"
      }
    },
    "osano:index:getCookieConsentRules": {
      "description": "Lists the classification rules of a Cookie Consent configuration, optionally filtered by storage type and classification. Useful for auditing rules managed outside Pulumi or finding rule IDs to import.",
      "inputs": {
        "properties": {
          "classification": {
            "type": "string",
            "description": "Only return rules with this classification: ANALYTICS, BLACKLISTED, ESSENTIAL, HIDDEN, MARKETING, or PERSONALIZATION."
          },
          "configId": {
            "type": "string",
            "description": "The Osano Cookie Consent config ID whose rules are listed."
          },
          "storeType": {
            "type": "string",
            "description": "Only return rules of this storage type: cookies, scripts, iframes, or localStorage."
          }
        },
        "type": "object",
        "required": [
          "configId"
        ]
      },
      "outputs": {
        "properties": {
          "configId": {
            "description": "The config ID that was queried.",
            "type": "string"
          },
          "rules": {
            "description": "Every matching rule, across all pages.",
            "items": {
              "$ref": "#/types/osano:index:CookieConsentRuleDetails"
            },
            "type": "array"
          }
        },
        "required": [
          "configId",
          "rules"
        ],
        "type": "object"
      }
    },
    "osano:index:getSession": {
      "description": "Resolves a Unified Consent session ID to its subject's verified ID and profile, using the Unified Consent API key.",
      "inputs": {
        "properties": {
          "sessionId": {
            "type": "string",
            "description": "The session ID to resolve.",
            "secret": true
          }
        },
        "type": "object",
        "required": [
          "sessionId"
        ]
      },
      "outputs": {
        "properties": {
          "exists": {
            "description": "Whether Osano recognized the session.",
            "type": "boolean"
          },
          "profile": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The session's profile (email, firstName, lastName). Secret, because it is personal data.",
            "secret": true,
            "type": "object"
          },
          "verifiedId": {
            "description": "The verified ID of the session's subject.",
            "type": "string"
          }
        },
        "required": [
          "exists",
          "verifiedId",
          "profile"
        ],
        "type": "object"
      }
    },
    "osano:index:getSubject": {
      "description": "Fetches subject identifiers (subject, verified, anonymous IDs) for a reference or session ID.",
      "inputs": {
        "properties": {
          "referenceType": {
            "type": "string",
            "description": "Reference type: subject (default) for a subject's verified or anonymous ID, or session for a session ID. anonymous is accepted as a deprecated alias of subject."
          },
          "subjectRef": {
            "type": "string",
            "description": "The subject reference to resolve: an anonymous ID, verified ID, or session ID."
          }
        },
        "type": "object",
        "required": [
          "subjectRef"
        ]
      },
      "outputs": {
        "properties": {
          "anonymousId": {
            "description": "The subject's anonymous ID, if any.",
            "type": "string"
          },
          "exists": {
            "description": "Whether Osano knows the subject. The ID outputs are empty when false.",
            "type": "boolean"
          },
          "subjectId": {
            "description": "The subject's Osano ID.",
            "type": "string"
          },
          "subjectRef": {
            "description": "The subject reference that was resolved.",
            "type": "string"
          },
          "verifiedId": {
            "description": "The subject's verified ID, if the subject is verified.",
            "type": "string"
          }
        },
        "required": [
          "subjectRef",
          "subjectId",
          "verifiedId",
          "anonymousId",
          "exists"
        ],
        "type": "object"
      }
    },
    "osano:index:getSubjectProfile": {
      "description": "Reads a subject's profile (email and subject ID) using the Unified Consent API key. The outputs are secrets because they hold personal data.",
      "inputs": {
        "properties": {
          "subjectId": {
            "type": "string",
            "description": "The subject ID whose profile is returned."
          }
        },
        "type": "object",
        "required": [
          "subjectId"
        ]
      },
      "outputs": {
        "properties": {
          "email": {
            "description": "The subject's email address. Secret, because it is personal data.",
            "secret": true,
            "type": "string"
          },
          "exists": {
            "description": "Whether Osano returned a profile for the subject.",
            "type": "boolean"
          },
          "profile": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The complete profile Osano returned. Secret, because it is personal data.",
            "secret": true,
            "type": "object"
          },
          "subjectId": {
            "description": "The subject ID that was looked up.",
            "type": "string"
          }
        },
        "required": [
          "subjectId",
          "exists",
          "email",
          "profile"
        ],
        "type": "object"
      }
    },
    "osano:index:getUnifiedConsent": {
      "description": "Fetches the unified consent state for a subject reference using the Unified Consent API key.",
      "inputs": {
        "properties": {
          "countryCodeOverride": {
            "type": "string",
            "description": "Optional ISO 3166-1 country code Osano uses instead of resolving the caller's IP address, which in a pipeline is the CI runner's."
          },
          "referenceType": {
            "type": "string",
            "description": "Reference type: subject (default) for a subject's verified or anonymous ID, or session for a session ID. anonymous is accepted as a deprecated alias of subject."
          },
          "regionCodeOverride": {
            "type": "string",
            "description": "Optional ISO 3166-2 region code Osano uses instead of resolving the caller's IP address."
          },
          "subjectRef": {
            "type": "string",
            "description": "The subject reference to look up: an anonymous ID, verified ID, or session ID."
          }
        },
        "type": "object",
        "required": [
          "subjectRef"
        ]
      },
      "outputs": {
        "properties": {
          "conflicts": {
            "description": "Conflicting consents Osano resolved, with the resolution and the actions in conflict.",
            "items": {
              "additionalProperties": {
                "$ref": "pulumi.json#/Any"
              },
              "type": "object"
            },
            "type": "array"
          },
          "exists": {
            "description": "Whether Osano has any consent for the subject.",
            "type": "boolean"
          },
          "subjectRef": {
            "description": "The subject reference that was looked up.",
            "type": "string"
          },
          "unifiedConsent": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The merged consent: subjectId, brandId, channelIds, jurisdiction, lastUpdateDate, lastConflictDate, actions, attributes, compliance, and tags.",
            "type": "object"
          }
        },
        "required": [
          "subjectRef",
          "exists",
          "unifiedConsent",
          "conflicts"
        ],
        "type": "object"
      }
    },
    "osano:index:sendSubjectCode": {
      "description": "Sends a verification code to a subject's email or phone, authenticating with every configured key (the Osano API key, the Unified Consent API key, or both). Pulumi runs invokes on every preview, update, and refresh, so declaring this in a stack sends a new code each time; call it from automation rather than from long-lived stack code.",
      "inputs": {
        "properties": {
          "email": {
            "type": "string",
            "description": "Email address to send the code to. Set exactly one of email or phone."
          },
          "hashedSubjectId": {
            "type": "string",
            "description": "Optional hashed subject identifier, sent only when set. Osano's current API identifies the subject by email or phone."
          },
          "phone": {
            "type": "string",
            "description": "Phone number to send the code to by SMS. Set exactly one of email or phone."
          }
        },
        "type": "object"
      },
      "outputs": {
        "properties": {
          "channel": {
            "description": "The delivery channel: email or sms.",
            "type": "string"
          },
          "destination": {
            "description": "The email address or phone number the code was sent to. Secret, because it is personal data.",
            "secret": true,
            "type": "string"
          },
          "hashedSubjectId": {
            "description": "The hashed subject identifier sent with the request, if any.",
            "type": "string"
          },
          "session": {
            "description": "The SMS challenge session, when Osano returns one; pass it to verifySubjectCode. Empty for email.",
            "secret": true,
            "type": "string"
          }
        },
        "required": [
          "hashedSubjectId",
          "channel",
          "destination",
          "session"
        ],
        "type": "object"
      }
    },
    "osano:index:verifySubjectCode": {
      "description": "Verifies a subject profile using the code sent via email or SMS. Pulumi runs invokes on every preview, update, and refresh, and one-time codes cannot be reused, so call this from automation rather than from long-lived stack code.",
      "inputs": {
        "properties": {
          "code": {
            "type": "string",
            "description": "The one-time verification code the subject received (6 characters by email, 8 by SMS).",
            "secret": true
          },
          "email": {
            "type": "string",
            "description": "Email address the code was sent to. Set exactly one of email or phone."
          },
          "hashedSubjectId": {
            "type": "string",
            "description": "Optional hashed subject identifier, sent only when set."
          },
          "phone": {
            "type": "string",
            "description": "Phone number the code was sent to. Set exactly one of email or phone."
          },
          "session": {
            "type": "string",
            "description": "The SMS challenge session. Required with phone; not used with email.",
            "secret": true
          }
        },
        "type": "object",
        "required": [
          "code"
        ]
      },
      "outputs": {
        "properties": {
          "channel": {
            "description": "The verification channel: email or sms.",
            "type": "string"
          },
          "destination": {
            "description": "The email address or phone number that was verified. Secret, because it is personal data.",
            "secret": true,
            "type": "string"
          },
          "hashedSubjectId": {
            "description": "The hashed subject identifier sent with the request, if any.",
            "type": "string"
          },
          "profile": {
            "additionalProperties": {
              "$ref": "pulumi.json#/Any"
            },
            "description": "The complete response Osano returned. Secret, because it can hold personal data.",
            "secret": true,
            "type": "object"
          },
          "verified": {
            "description": "True when Osano accepted the code; a rejected code fails the invoke instead.",
            "type": "boolean"
          },
          "verifiedId": {
            "description": "The subject's verified ID returned by Osano.",
            "type": "string"
          }
        },
        "required": [
          "hashedSubjectId",
          "channel",
          "destination",
          "verified",
          "verifiedId",
          "profile"
        ],
        "type": "object"
      }
    }
  }
}
