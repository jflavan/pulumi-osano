version: 2
updates:
  # Keeps the SHA-pinned actions current; Dependabot rewrites the hash and the version comment together.
  - package-ecosystem: github-actions
    directories:
      - /
      - /.github/actions/*
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - "*"
