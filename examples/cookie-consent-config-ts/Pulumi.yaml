name: cookie-consent-config-ts
runtime: nodejs
description: Minimal example deploying an Osano Cookie Consent config and rule
config:
  osano:osanoApiKey:
    secret: true
plugins:
  providers:
    - name: osano
      path: ../../bin
