name: text-customization-ts
runtime: nodejs
description: Example customizing consent dialog text for en-US
config:
  osano:osanoApiKey:
    secret: true
plugins:
  providers:
    - name: osano
      path: ../../bin
